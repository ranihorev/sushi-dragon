# React Native's autolinking and CocoaPods both walk node_modules expecting a
# flat tree. pnpm's default isolated layout hides transitive native modules
# behind symlinks, and the build dies during setup rather than saying why —
# which is what happened here. Same setting chess-buddy uses.
node-linker=hoisted
legacy-peer-deps=true
