/**
 * The restaurant's colours.
 *
 * A dark room with warm light in it, so the sushi — which is where his eyes
 * need to be — is the brightest thing on the screen. Everything else is
 * deliberately dim: a counter, a dragon, some lanterns, no decoration
 * competing with the word he is trying to read.
 */

export const NIGHT = '#0d1b2a';
export const INK = '#1b263b';

/** the counter he slides the sushi along */
export const WOOD = '#8b5e34';
export const WOOD_DARK = '#5c3d22';

/** sushi */
export const RICE = '#f7f3e8';
export const NORI = '#2b3a2f';
export const SALMON = '#ff8f5e';
export const ROE = '#ff6b35';

/**
 * The dragon's green, which is the only part of it still described here.
 *
 * It used to be drawn in code from half a dozen of these. It is five rendered
 * images now, so the rest of the palette went with them — this one stays
 * because the parent screen colours a word's progress bar with it once the
 * word is his.
 */
export const SCALE = '#7fd4ab';

/** dragon fire, which is also the aburi torch that sears a new word */
export const FIRE = '#ff9f1c';
export const FIRE_HOT = '#ffdd57';

/** the dab that marks a letter which doesn't say what it should */
export const WASABI = '#7cb518';

export const LANTERN = '#ffb703';
export const CREAM = '#fff8e7';

/** the word itself, and the seams between its pieces */
export const TEXT = '#2b2118';
export const SEAM = 'rgba(43, 33, 24, 0.28)';
